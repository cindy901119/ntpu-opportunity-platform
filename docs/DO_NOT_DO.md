# DO_NOT_DO

- 不要把講座／工作坊放回第一版核心。
- 不要把實習／職缺放回第一版核心。
- 不要把打工放回第一版核心。
- 不要把海外交換放回第一版核心。
- 不要把一般活動放回第一版核心。
- 不要新增學院輸入欄位。
- 不要新增就讀階段輸入欄位。
- 不要在使用者端顯示 internalScore 或推薦分數。
- 不要在使用者端顯示加權數字、加幾分或排名數字。
- 不要把需組隊放進「我的資格」。
- 不要自行大量新增 label。
- 不要把主題領域改成單選。
- 不要把「尚未截止」放進截止時間 filter。
- 不要把總獎金範圍當成最高獎金。
- 不要把作品形式做成第一版主要 filter。
- 不要把「查看官方簡章」寫成「立即報名」或「前往報名」。
