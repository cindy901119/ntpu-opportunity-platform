# Issues

## 2026-06-24

### Fixed

- 偏好 chip 選取後文字與背景對比不足。
- 卡片「為什麼推薦？」展開區過度像推薦演算法說明，已改成短版「資格符合」與「偏好交集」。
- 對外頁面仍顯示「獎金獵人」，已暫改為「北大機會雷達」。
- 卡片右上角重複顯示資格狀態，已移除。
- 卡片主要操作一度誤改為「查看官方簡章」優先；已修正為列表卡片「查看詳情」優先，官方簡章為次要連結。

### Open

- 最高獎金級距與獎勵形式 label 仍需使用者測試後確認。
- mock data 的 `sourceUrl` 仍是 demo URL，v0.2 若進一步驗證真實流程需換成真實官方簡章來源。

### Environment

- Codex 沙盒內仍無法直接跑 `npm run dev` / `npm run lint`，原因是 Node 子程序讀取 `C:\Users\xin` 時被權限限制擋下。
